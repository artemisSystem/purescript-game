{ "private": true
, "scripts":
  { "test": "spago test"
  , "example-AffGame": "spago build && (parcel serve --open -p 1337 --out-dir dev test/Test/AffGame/index.html & spago build --watch)"
  , "example-Event": "spago build && (parcel serve --open -p 1337 --out-dir dev test/Test/Event/index.html & spago build --watch)"
  }
}
